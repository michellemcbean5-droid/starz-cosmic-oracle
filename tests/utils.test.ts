import { describe, it, expect } from 'vitest'

describe('Example utilities', () => {
  it('basic math works', () => {
    expect(2 + 2).toBe(4)
  })

  it('string concatenation works', () => {
    expect('starz' + 'oracle').toBe('starzoracle')
  })
})
