# Optional: enables the full AI-written "Ask The Oracle" reading (/api/reading/ai).
# Without this, the engine still returns the complete rules-based slang reading —
# just not the free-flowing Claude-authored version.
ANTHROPIC_API_KEY=your_anthropic_api_key
